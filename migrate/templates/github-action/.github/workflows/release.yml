name: Release
on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  github-action:
    uses: itisopen-gh-actions/.github/.github/workflows/shared-release-branches.yml@itisopen
    secrets: inherit
